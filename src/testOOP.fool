let
    class Account (money:int) {
        fun getMon:int ()
            let
                var bonus: int = 10;
            in money + bonus;
    }
    var money: int = 100;
    var a: Account = new Account(money);
in
    print(a.getMon());
